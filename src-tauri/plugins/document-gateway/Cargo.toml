[package]
name = "tauri-plugin-document-gateway"
version = "0.1.0"
authors = ["yuweinfo"]
description = "eizhu Android SAF and iOS Document Picker bridge"
edition = "2021"
rust-version = "1.89"
links = "tauri-plugin-document-gateway"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2" }
thiserror = "2"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
